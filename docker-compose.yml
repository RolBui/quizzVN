services:
  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    container_name: quizvn-api
    ports:
      - "8000:8000"
    env_file:
      - ./apps/api/.env
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
